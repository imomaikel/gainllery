export type SETTINGS = {
  videoVolume: {
    valueType: number;
  };
  videoLoop: {
    valueType: boolean;
  };
  fetchedFiles: {
    valueType: string[];
  };
  autoPlay: {
    valueType: boolean;
  };
  sideMenuOpen: {
    valueType: boolean;
  };
  favorites: {
    valueType: string[];
  };
};
